import { Stack } from 'expo-router';

const LinksLayout = () => {
  return (
    <Stack screenOptions={{}}>
      <Stack.Screen name="links" options={{}} />
    </Stack>
  );
};

export default LinksLayout;
